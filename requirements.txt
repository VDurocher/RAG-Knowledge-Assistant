# Core RAG pipeline
langchain>=0.3.25
langchain-community>=0.3.23
langchain-openai>=0.2.14
langchain-text-splitters>=0.3.8

# Vector store
faiss-cpu>=1.9.0

# Document loading
pypdf>=5.4.0

# Local embeddings (optional — only needed when EMBEDDER_TYPE=local)
sentence-transformers>=3.4.0

# UI
streamlit>=1.44.0

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.9.0

# Additional document formats
docx2txt>=0.8

# Security — uploaded filename sanitisation
werkzeug>=3.1.0

# Hybrid BM25 + semantic search (optional — required if HYBRID_SEARCH=true)
rank-bm25>=0.2.2
