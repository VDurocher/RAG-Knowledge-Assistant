# Core RAG pipeline
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0

# Vector store
faiss-cpu>=1.9.0

# Document loading
pypdf>=5.0.0

# Local embeddings (optional — only needed when EMBEDDER_TYPE=local)
sentence-transformers>=3.3.0

# UI
streamlit>=1.40.0

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.8.0

# Formats de document additionnels
docx2txt>=0.8

# Sécurité — nettoyage des noms de fichiers uploadés
werkzeug>=3.0.0

# Recherche hybride BM25 + sémantique (optionnel — requis si HYBRID_SEARCH=true)
rank-bm25>=0.2.2

